instalar o jupyter no visual studio
instalar o python no computador e no visual studio
instalar o kernel
Instalar o pandas no visual studio - abrir o terminal do visual studio e digitar o comando abaixo:
pip install pandas

sempre que abrir novamente o notebook depois de ter fechado ele executar todo o código
novamente, para que ele importe novamente todos os dados.
Para isso, executar o comando "Run All".
